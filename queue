import java.util.*;
//Queue using Linked List
public class QLL{
    Node head;

    class Node{
        int data;
        Node next;

        Node(int data){
            this.data=data;
            this.next=null;
        }
    }

//addlast
    public void enqueue(int data){
        Node newnode=new Node(data);
        
        if (head==null){
            head=newnode;
            System.out.println("eq:"+head.data);
            return;
        }

        Node cur=head;
        while(cur.next!=null){
            cur=cur.next;
        }
        cur.next=newnode;
        System.out.println("eq:"+cur.next.data);
    
    }

//deletefirst
    public void dequeue(){
        if(head==null){
            System.out.print("Que is Empty");
            return;
        }
        if(head.next==null){
            System.out.println("deq:"+head.data);
            head=null;
            return;
        }
        System.out.println("deq:"+head.data);
        head=head.next;
        
        
    }






    public static void main(String[] args){
        QLL cl=new QLL();
        cl.enqueue(10);
        cl.enqueue(20);
        cl.enqueue(30);
        cl.dequeue();
        cl.dequeue();
        cl.dequeue();
        //cl.dequeue();
    }
}
